{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "kompajliranje",
            "type": "shell",
            "command": "g++ -g glupiTest.cpp -lPocoFoundation -lPocoNet -lpthread",
            "group": {
                "kind": "build",
                "isDefault": true
            } 
        }
    ]
}